# Example .env file for MyVNC Docker container
# Copy this file to .env and adjust values as needed

# Hostname for the MyVNC instance (accessible from browser)
MYVNC_HOSTNAME=myvnc.example.com

# Path to LSF environment file (used for LSF job submission)
LSF_ENV_FILE=/path/to/lsf/profile.lsf

# Optional VNC-specific environment variables
# VNC_PORT_RANGE_START=6400
# VNC_PORT_RANGE_END=6499 